name: CI Pipeline

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  lint-and-test:
    name: Lint and Test
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: './SISIN_UTPL/package-lock.json'
      
      - name: Install dependencies
        run: npm ci
        working-directory: ./SISIN_UTPL
      
      - name: Run linting (TypeScript check)
        # 👇 CAMBIO AQUÍ: Usamos '-p typescript' para evitar el error
        run: npx -p typescript tsc --noEmit
        working-directory: ./SISIN_UTPL
      
      - name: Run tests
        run: npm test -- --no-watch --browsers=ChromeHeadless --code-coverage
        working-directory: ./SISIN_UTPL
        continue-on-error: true
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: ./SISIN_UTPL/coverage/lcov.info
          fail_ci_if_error: false

  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: lint-and-test
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: './SISIN_UTPL/package-lock.json'
      
      - name: Install dependencies
        run: npm ci
        working-directory: ./SISIN_UTPL
      
      - name: Build application
        run: npm run build -- --configuration=production
        working-directory: ./SISIN_UTPL
      
      - name: Check build artifacts
        run: |
          if [ ! -d "SISIN_UTPL/dist/riskless-diem" ]; then
            echo "Build failed: dist directory not found"
            exit 1
          fi
          echo "Build successful. Artifacts in SISIN_UTPL/dist/riskless-diem"
          ls -lah SISIN_UTPL/dist/riskless-diem/
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: SISIN_UTPL/dist/riskless-diem
          retention-days: 7

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: './SISIN_UTPL/package-lock.json'
      
      - name: Install dependencies
        run: npm ci
        working-directory: ./SISIN_UTPL
      
      - name: Run npm audit
        run: npm audit --audit-level=moderate
        working-directory: ./SISIN_UTPL
        continue-on-error: true
